import type { CSSUtility } from '@/css-utilities';
import type { LocalPluginAPI } from '@/common';
declare class TransformStyle implements CSSUtility {
    defaultTheme: {
        flat: string;
        '3d': string;
    };
    utilities: ({ matchUtilities, theme }: LocalPluginAPI) => void;
}
export declare const transformStyle: TransformStyle;
export {};

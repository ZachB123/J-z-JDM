import type { CSSUtility } from '@/css-utilities';
import type { LocalPluginAPI } from '@/common';
declare class TransformCore implements CSSUtility {
    utilities: ({ addUtilities }: LocalPluginAPI) => void;
}
export declare const transformCore: TransformCore;
export {};

import type { CSSUtility } from '@/css-utilities';
import type { LocalPluginAPI } from '@/common';
declare class TransformBox implements CSSUtility {
    defaultTheme: {
        content: string;
        border: string;
        fill: string;
        stroke: string;
        view: string;
    };
    utilities: ({ matchUtilities, theme }: LocalPluginAPI) => void;
}
export declare const transformBox: TransformBox;
export {};

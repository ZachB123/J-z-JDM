import type { CSSAnimation } from '@/css-animations';
import type { PluginUtils } from 'tailwindcss/types/config';
type Value = string;
type Values = Record<string, Value>;
declare class Spin implements CSSAnimation {
    private isProcessableValue;
    private isProcessableValues;
    private isValues;
    private normaliseValues;
    defaultTheme: Values;
    private xyKeyframes;
    private zKeyframes;
    keyframes: () => {};
    animation: ({ theme }: PluginUtils) => {};
}
export declare const spin: Spin;
export {};
